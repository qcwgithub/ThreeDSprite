
public class LocOnDisconnect : OnDisconnect {
    // handle(server: LocServer, msg: { object socket, isListen: boolean, isServer: boolean }) {
    //     super.handle(server, msg);

    //     server.map.forEach((value: LocServerInfo, int key, m: Dictionary<number, LocServerInfo>) => {
    //         if (value.socket == socket) {
    //             value.socket = null;
    //             server.logger.info("-" + value.id);
    //         }
    //     });

    //     return ECode.Success;
    // }
}